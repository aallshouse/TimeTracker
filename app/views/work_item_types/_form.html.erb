<%= form_for(@work_item_type) do |f| %>
  <% if @work_item_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@work_item_type.errors.count, "error") %> prohibited this work_item_type from being saved:</h2>

      <ul>
      <% @work_item_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
